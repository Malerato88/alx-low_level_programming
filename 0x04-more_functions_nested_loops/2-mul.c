#include "main.h"

/**
 * mul - multiplies two integers
 * @b: The first number to multiply
 * @c: The second number to multiply with first number
 * Return: The results of multiplication between b param and c param
 */

int mul(int b, int c)
{
	return (b * c);
}
